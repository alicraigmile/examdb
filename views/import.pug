doctype  html
html
	head
		title Import exams data | ExamDB
		meta(charset='UTF-8')
		meta(name='description' content='You can import data about examinations into ExamDB. Simply upload a CSV file.')
		link(rel='stylesheet' href='/examdb.css')
		script(src='/library/jquery/jquery.min.js')
		script(src='/library/jquery-tablesort/jquery.tablesort.min.js')
		meta(name='viewport' content='width=device-width, initial-scale=1')
	body

		header
			p
				a(href='/' title='ExamDB Homepage')
					img(src='/examdb.png' width='64' height='64' alt='')

		if message
			div(class='message message-'+status)
				
				span
					strong #{headline}
					= ': '
					| #{message}
				span(class='buttonContainer')
					button(class='close' title='close') ❌ Ok 

		section

			h1 Import examinations data

			h2 CSV Upload

			p You can import data about examinations into ExamDB.

			p It's really simple, there are only a couple of steps.

			form(method='post' action='/exams/upload' encType='multipart/form-data')
				div
					label(for='file') 
						| 1. Select a CSV file 
					input(type='file' name='file')
				div
					| 2. Send it to us! 
					input(type='submit' name='' value='Upload')
		script
			| $('.message .close').click(function(e) { $('.message').slideUp() });
